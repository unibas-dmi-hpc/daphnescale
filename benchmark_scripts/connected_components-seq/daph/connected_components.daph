start_reading = now();
G = readMatrix($f);
start_compute=now();
maxi = 100;
c = seq(1.0, as.f64(nrow(G)), 1.0); // init w/ vertex IDs
for(iter in 1:maxi) {
    x = aggMax(G * t(c), 0);
    c = max(x, c);

}
end = now();
duration_reading = (end - start_reading) / 1000000000.0;
duration_compute = (end - start_compute) / 1000000000.0;
print(duration_reading, 0); print(",",0); print(duration_compute, 0); print(",",0); print(sum(c));
